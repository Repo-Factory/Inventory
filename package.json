{
    "name": "server",
    "version": "1.0.0",
    "main": "index.js",
    "scripts": 
    {
      "start": "sudo rm -r db-data && sudo docker-compose up --build",
      "detach": "sudo docker-compose up --build -d",
      "stop" : "sudo docker-compose stop"
    }
  }